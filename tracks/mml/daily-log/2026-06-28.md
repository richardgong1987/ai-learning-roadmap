# MML Daily Log

Date: 2026-06-28

## Completed Lesson 28

Completed Lesson:
28. PCA

Key Insight:
PCA finds the most important directions in data. It reduces many features into fewer new features while keeping as much variance as possible.

Concrete Example:
If points on a 2D chart mostly stretch along one diagonal line, PCA finds that diagonal as the first principal component. The data can then be described mainly by positions along that direction.

Main Formula Ideas:

Center the data:

\[
x_i' = x_i - \bar{x}
\]

Build the covariance matrix:

\[
\Sigma = \frac{1}{n}X^T X
\]

Solve the eigenvalue equation:

\[
\Sigma v = \lambda v
\]

ML Connection:
PCA is used for dimensionality reduction, visualization, compression, noise reduction, and reducing feature redundancy.

## Completed Lesson 29

Completed Lesson:
29. Gradient Descent

Key Insight:
Gradient descent reduces loss step by step by moving parameters in the opposite direction of the gradient.

Concrete Example:
For L(w) = (w - 3)^2, starting at w = 0 with learning rate 0.1 gives gradient -6 and update w_new = 0 - 0.1(-6) = 0.6. The parameter moves toward the minimum at w = 3.

Main Formula:

\[
\theta_{new} = \theta_{old} - \alpha \nabla L(\theta_{old})
\]

ML Connection:
Gradient descent trains machine learning models by repeatedly updating weights and biases to reduce loss.

## Completed Lesson 30

Completed Lesson:
30. Neural Networks

Key Insight:
A neural network is a model made from connected neurons. Each neuron takes inputs, multiplies them by weights, adds a bias, applies an activation function, and passes the result forward.

Concrete Example:
For one neuron with x1 = 2, x2 = 3, w1 = 0.5, w2 = 1, and b = -1, the pre-activation value is z = 0.5*2 + 1*3 - 1 = 3. With ReLU activation, a = max(0, 3) = 3.

Main Formula:

\[
z = Wx + b
\]

\[
a = \sigma(z)
\]

ML Connection:
Neural networks use linear algebra for layer calculations, calculus and the chain rule for backpropagation, and gradient descent to update weights and biases.

Track Status:
Mathematics for Machine Learning track completed.
